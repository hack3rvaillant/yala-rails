<nav class="navbar">
  <%= render ContainerComponent.new(classes: "px-4") do %>
    <div class="flex">
      <%= render NavLogoComponent.new %>
      <ul class="menu">
        <% @main_menu_items.each do |item| %>
          <%= render MenuItemComponent.new(
              label: item.label,
              path: item.path,
              active: active?(item.path)
            )
          %>
        <% end %>
        <%#= render AccountDropdownComponent.new(@user) %>
      </ul>
    </div>
  <% end %>
</nav>
